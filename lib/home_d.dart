import 'package:flutter/material.dart';

class home_d extends StatelessWidget {
  const home_d({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
        child: Text(
      'Calls & Videos ',
      style: TextStyle(
          color: Colors.black, fontSize: 25, fontWeight: FontWeight.bold),
    ));
  }
}
